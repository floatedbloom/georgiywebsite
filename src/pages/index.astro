---
import Layout from "../layouts/Layout.astro"

title: "Welcome"
layout: Layout
description: "This is the home page."
---

<Layout title="Homepage • Georgiy">
	<head>
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>
		<title>Welcome!</title>
		<style>
			/* Normalize Bootstrap element spacing within this page */
			main :where(h1, h2, h3, h4, h5, h6, p, ul, ol) { margin-top: 0; margin-bottom: 0; }
			main ul, main ol { padding-left: 1rem; }

			main {
				margin: auto;
				padding: 1rem;
				width: 100%;
				max-width: 800px; /* Adjusted for better scaling */
				color: rgb(19, 18, 18);
				font-size: 1.25rem; /* Converted to rem for better scalability */
				line-height: 1.6;
				/* Center content without extra wrappers */
				display: grid;
				place-items: center;
				min-height: 100svh;
			}

			/* Responsive main container */
			@media (max-width: 768px) {
				main {
					padding: 0.5rem;
					font-size: 1rem;
				}
			}

			@media (max-width: 480px) {
				main {
					padding: 0.25rem;
					font-size: 0.9rem;
				}
			}

			.astro-a {
				position: absolute;
				top: -2rem; /* Adjusted to rem */
				left: 50%;
				transform: translateX(-50%);
				width: 13.75rem; /* Converted to rem */
				height: auto;
				z-index: -1;
			}
			h1 {
				font-size: 2.5rem; /* Reduced size for better fit on mobile */
				font-weight: 700;
				line-height: 1;
				text-align: center;
				margin-bottom: 1em;
			}

			/* Desktop-only h1 size reduction */
			@media (min-width: 1025px) {
				h1 {
					font-size: 2rem; /* Reduced from 2.5rem for desktop */
				}
			}

			/* Responsive h1 sizing */
			@media (max-width: 768px) {
				h1 {
					font-size: 2rem;
				}
			}

			@media (max-width: 480px) {
				h1 {
					font-size: 1.5rem;
				}
			}

			.instructions {
				margin-bottom: 2rem;
				border: 1px solid rgba(var(--accent-light), 25%);
				background: linear-gradient(rgba(var(--accent-dark), 66%), rgba(var(--accent-dark), 33%));
				padding: 1.5rem;
				border-radius: 0.5rem; /* Converted to rem */
			}
			.instructions code {
				font-size: 0.8em; /* Already relative */
				font-weight: bold;
				background: rgba(var(--accent-light), 12%);
				color: rgb(var(--accent-light));
				border-radius: 0.25rem; /* Converted to rem */
				padding: 0.3em 0.4em;
			}
			.instructions strong {
				color: rgb(var(--accent-light));
			}
			.link-card-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(12ch, 1fr)); /* Adjusted for smaller screens */
				gap: 1rem; /* Reduced gap for better spacing on mobile */
				padding: 0;
			}
			.text-gradient {
				background-image: var(--accent-gradient);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-size: 400%;
				background-position: 0%;
			}

			/* Hero role line (simple, no glitch) */
			.role { font-weight: 700; font-size: clamp(1.5rem, 6vw, 3rem); color: white; text-align: center; }
			.btn-custom {
				margin: 0.5rem 0; /* Add margin for spacing between buttons on mobile */
				width: 100%; /* Make buttons full-width on small screens */
			}
			.name {
				font-size: clamp(3rem, 8vw, 8rem); /* Doubled from previous sizes - 100% bigger */
			}

			.tagline {
				margin-top: 1rem;
				margin-bottom: 0.5rem;
				font-size: clamp(1rem, 2.5vw, 1.25rem);
				color: rgba(255,255,255,0.9);
			}

			.cta-row {
				display: flex;
				gap: 0.75rem;
				justify-content: center;
			}

			/* Desktop-only name size reduction */
			@media (min-width: 1025px) {
				.name {
					font-size: clamp(2.5rem, 6vw, 6rem); /* Reduced max size for desktop */
				}
			}

			/* Responsive name sizing for different screen sizes */
			@media (max-width: 768px) {
				.name {
					font-size: clamp(5rem, 20vw, 12rem); /* Doubled tablet size */
				}
			}

			@media (max-width: 480px) {
				.name {
					font-size: clamp(4rem, 16vw, 8rem); /* Doubled mobile size */
				}
			}

			/* Contact icon row */
			.contact-icons { display:flex; gap:0.75rem; margin-top:0.9rem; flex-wrap:wrap; justify-content:center; }
			.icon-link { display:inline-grid; place-items:center; width:44px; height:44px; border-radius:999px; background:rgba(255,255,255,0.14); border:1px solid rgba(255,255,255,0.25); color:#fff; transition:transform .15s ease, background .2s ease, border-color .2s ease; text-decoration:none; }
			.icon-link:hover { transform:scale(1.06); background:rgba(255,255,255,0.22); border-color:rgba(255,255,255,0.35); text-decoration:none; }
			.icon-link:focus { text-decoration:none; }
			.icon-link svg { width:22px; height:22px; }
			.icon-img { width:20px; height:20px; filter: invert(1) brightness(1000%); }
			.tech-icon { margin-bottom: 0; line-height: 1; display: inline-block; }
			.tech-icon.emoji { transform: translateY(1px); }

			/* Adjust for mobile top navigation */
			@media (max-width: 768px) {
				main {
					padding-top: 100px;
					margin-top: -50px; /* Compensate for the added top padding */
				}
				
				.name {
					line-height: 1.1 !important; /* Increase line-height to prevent clipping */
				}
			}
		</style>
	</head>
	<body class="bg-dark text-light">
		<main>
			<div>
					<h1 class="text-gradient mb-2 name">Georgiy Lapin</h1>
					<div class="role">Full Stack</div>
					<p class="tagline text-light">(Building ∪ Learning)</p>
					<div class="contact-icons" aria-label="Contact links">
						<a class="icon-link" href="mailto:georgelapin23@gmail.com" aria-label="Email">
							<span class="tech-icon emoji">✉️</span>
						</a>
						<a class="icon-link" href="https://github.com/floatedbloom" target="_blank" rel="noopener noreferrer" aria-label="GitHub">
							<i class="tech-icon devicon-github-original"></i>
						</a>
						<a class="icon-link" href="https://www.linkedin.com/in/georgiylapin/" target="_blank" rel="noopener noreferrer" aria-label="LinkedIn">
							<i class="tech-icon devicon-linkedin-plain"></i>
						</a>
						<a class="icon-link" href="https://leetcode.com/u/floatedbloom/" target="_blank" rel="noopener noreferrer" aria-label="LeetCode">
							<i class="tech-icon devicon-leetcode-plain"></i>
						</a>
					</div>
			</div>
		</main>	
	</body>

</Layout>
